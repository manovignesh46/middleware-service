export enum ResponseMessage {
  SUCCESS = 'Success',
  PUSH_STATUS_SUCCESS = 'Device token registered successfully.',
  PUSH_STATUS_NO_RECORD = 'Push Id is not valid.',
  PUSH_STATUS_INVALID_STATUS = 'Push status is invalid.',
}
